# provides --MARK-- message capability
$ModLoad immark

# provider RELP syslog reception
module(load="imrelp")

input(type="imrelp" port="514" tls="on")

$template HostProgramName, "/var/log/hosts/<%= @environment_type %>/%HOSTNAME%/%PROGRAMNAME%.log"
*.* ?HostProgramName